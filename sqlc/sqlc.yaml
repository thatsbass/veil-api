version: "2"
sql:
  - engine: "postgresql"
    queries: "queries/"
    schema: "../migrations/"
    gen:
      go:
        package: "db"
        out: "../internal/db"
        emit_json_tags: true
        emit_pointers_for_null_types: true
        emit_prepared_queries: false
